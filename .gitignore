# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build
/dist

# Next.js
/.next
/out
next-env.d.ts
*.tsbuildinfo

# NOTE: /docs is intentionally NOT ignored — it is the static export that
# GitHub Pages serves (Settings → Pages → Deploy from branch → /docs).

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
public/.DS_Store
